import { FavoriteDirectorPipe } from './favorite-director.pipe';

describe('FavoriteDirectorPipe', () => {
  it('create an instance', () => {
    const pipe = new FavoriteDirectorPipe();
    expect(pipe).toBeTruthy();
  });
});
