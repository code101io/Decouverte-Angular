import { MovieHighlightDirective } from './movie-highlight.directive';

describe('MovieHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new MovieHighlightDirective(null);
    expect(directive).toBeTruthy();
  });
});
